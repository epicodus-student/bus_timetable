<div class="head">
  <h1>Stops</h1>
  <p><%= link_to "New stop", new_stop_path %></p>
</div>

<div class="content">
  <ul>
    <% @stops.each do |stop| %>
      <li><%= link_to "#{stop.line.name} / #{stop.station.name}", stop_path(stop) %></a></li>
    <% end %>
  </ul>
</div>


