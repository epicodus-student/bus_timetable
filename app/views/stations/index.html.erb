<div class="head">
  <h1>Stations</h1>
  <p><%= link_to "New station", new_station_path %></p>
</div>

<div class="content">
  <ul>
    <% @stations.each do |station| %>
      <li><a href="/stations/<%= station.id %>"><%= Station.find(station).name %></a> </li>
    <% end %>
  </ul>
</div>

